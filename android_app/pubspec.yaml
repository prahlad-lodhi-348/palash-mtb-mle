name: palash_mtb_mle
description: PALASH MTB-MLE tablet application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  # Vosk offline STT for Hindi (add after confirming package availability)
  vosk_flutter: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/dictionaries/hindi_santhali_500.csv
